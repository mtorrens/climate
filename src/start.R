library(XML)
library(data.table)
library(gdata)
library(highcharter)
library(ggplot2)
library(viridis)
library(lubridate)
library(scales)

PATH <- '~/Desktop/dvis/'
INPDIR <- paste(PATH, 'inp/', sep = '')
DATDIR <- paste(PATH, 'data/', sep = '')
TMPDIR <- paste(PATH, 'temp/', sep = '')
SRCDIR <- paste(PATH, 'src/', sep = '')
